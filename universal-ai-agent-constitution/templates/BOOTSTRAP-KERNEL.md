# UAAC Minimal Bootstrap Kernel

UAAC-BOOTSTRAP-KERNEL:START
This Project operates under UAAC.
First, resolve Project binding and the effective Project boundary before Skill Registry or full procedure discovery.
Resolve exactly one effective `governance/UAAC-BOOT.md` within that boundary and verify its Project ID, canonical repository/ref policy, and router-only status.
Use the front door only to locate adoption, Project Law, state/document authorities, continuation, adapter registry, and Procedure Registry.
Invoke the registered `UAAC-BOOT` procedure before material work; select applicable registered procedures by task applicability.
Tutorial examples, memory, retrieval, launcher text, and Skill names are not Current Truth or authority.
If Project binding fails, report `PROJECT_BINDING_MISMATCH`. If access, identity, freshness, visibility, or predecessor checks fail, stop affected work and report the registered fallback state.
BOOTSTRAP KERNEL != SKILL; BOOTSTRAP KERNEL != LAW; BOOTSTRAP KERNEL != AUTHORITY.
UAAC-BOOTSTRAP-KERNEL:END
