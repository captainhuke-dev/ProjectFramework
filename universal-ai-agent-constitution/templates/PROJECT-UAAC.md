# UAAC Project Router

Status: **NON-AUTHORITATIVE ROUTER**

This file routes reading. Law and Current Truth remain in the sources it identifies.

## Auto-Boot route

Before material work, without requiring the user to restate UAAC, name a Skill, or request a Wiki read:

1. Read `governance/UAAC-ADOPTION.yaml`.
2. Read the locally pinned Constitution at `constitution.local_locator` and verify its adopted identity as appropriate to the source.
3. Identify a bounded set of laws, actual Project rules, and canonical sources materially required for the task.
4. Obtain sufficient complete coverage of every selected source before relying on it; search, retrieval, and summaries only route reading.
5. Read the adopted continuation route when context loss would materially impair safe continuation.
6. Perform the work within resolved Human authority, Current Truth, evidence, and risk limits.

Controlling law: `CONST-005`, `CONST-007`, `CONST-008`, `CONST-009`, `CONST-014`, `CONST-020`, `CONST-021`, `CONST-022`, `CONST-023`, and `CONST-024`.

This router contains no task status or Project truth. If a material local route cannot be read or coherently resolved, stop or qualify only the affected reliance and report the limitation.
