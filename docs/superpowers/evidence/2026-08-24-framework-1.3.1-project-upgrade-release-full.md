# Framework 1.3.1 Project Upgrade Command — RELEASE_FULL Evidence

Captured: `2026-08-24T19:53:29+07:00`

Task: `TASK-018`

Purpose: bind final Framework `1.3.1` / Schema `1.0.0` Project Upgrade command verification to the final unchanged Framework distribution candidate after whole-branch review corrections. This revision supersedes the earlier evidence state first committed at `5951738`; that earlier `64/64` run was correctly invalidated when review found that `SKILL.md` did not yet include the Framework `1.3.1` amendment as its first required amendment read.

This evidence records actual checks and observed Git identities. It does not treat commit as push and does not infer remote publication.

## Final verified distribution candidate

- Branch: `work/project-upgrade-command`
- Candidate HEAD: `c8c66d2b657f20421194e5e5c62a27abc4cac2f7`
- Candidate tree: `2963b6b7d7fc5d86da940a1b18fb0c27510a1465`
- `managing-project-source/` subtree: `2faf5046546c8c5559608d104903cc32669991ae`
- `README.md` blob: `cf9d2ab574bde8a36fbdb27fb54d2bb84bceff3b`
- Worktree before final affected verification and `RELEASE_FULL`: clean
- Framework release: `1.3.1`
- Project Source Schema: `1.0.0`
- Release format: `3`
- Latest amendment: `managing-project-source/references/framework-governance-amendment-260824-1708.md`

The evidence file itself is updated after the distribution verification. It is not a Framework distribution semantic mutation and therefore is not part of the verified distribution subtree above. No Framework distribution file may change after this final `RELEASE_FULL` without invalidating and rerunning the applicable evidence.

## Implementation and correction commits in the final candidate

- `2086e0d` — `test: add project upgrade command pressure scenarios`
- `01f56dd` — `docs: define framework 1.3.1 project upgrade command`
- `8853f43` — `feat: register project upgrade command governance`
- `d34d323` — `docs: propagate framework 1.3.1 project upgrade command`
- `f06e3be` — `docs: expose project upgrade command in launchers`
- `5951738` — initial release-evidence checkpoint for the pre-review candidate
- `9275f76` — `docs: complete project upgrade task tracking`
- `d971ac0` — `fix: align project task source lifecycle wording`
- `c8c66d2` — `fix: include framework 1.3.1 amendment in required reads`

The Task-source-only commits do not change the Framework distribution. Commit `c8c66d2` does change `SKILL.md`, so the earlier distribution evidence was invalidated and the verification suites below were rerun from scratch on the new clean candidate.

## Whole-branch review finding and correction

Whole-branch review found one Important distribution inconsistency: `FRAMEWORK-RELEASE.yaml` correctly pointed to `references/framework-governance-amendment-260824-1708.md`, but `SKILL.md → Required References` still began with the Framework `1.3.0` amendment `260823-1439`. That could cause an executor following `SKILL.md` literally to skip the latest normative amendment.

The issue was corrected in `c8c66d2`. The final verification suite now checks that the first Framework amendment in `SKILL.md → Required References` exactly matches `FRAMEWORK-RELEASE.yaml → latest_framework_amendment`.

A separate Task-source wording inconsistency was corrected in `d971ac0`: the durable Task source now explicitly supports completed Tasks instead of claiming it contains only not-yet-completed work. This correction is outside the Framework distribution and did not invalidate release verification.

## Final affected verification

Fresh affected verification on the corrected candidate returned:

`AFFECTED VERIFICATION PASS 57/57`

The 57 checks cover release identity, current starter metadata, historical-amendment preservation, registered command coverage, bounded command registry, comparison vocabulary, fresh-upstream/current-local-pin separation, upgrade-intent versus mutation approval, Direct-to-Latest path classes/preservation, Scenario 136–150 presence and normative mapping, launcher equality/size, reserved slots, prohibited artifact scope, and the new invariant that the first required Framework amendment in `SKILL.md` matches the release descriptor latest-amendment pointer.

## Final RELEASE_FULL result

One full current-distribution structural verification was rerun after the corrected Framework distribution candidate stopped changing.

Result:

`RELEASE_FULL PASS 65/65`

Launcher lengths observed in that run:

- ChatGPT launcher: `4495` Unicode characters
- Claude launcher: `4492` Unicode characters

The 65 passing checks cover:

- Framework `1.3.1` / Schema `1.0.0` / release format `3` identity;
- stable release channel and canonical repository identity;
- latest Framework amendment pointer and approval provenance;
- `SKILL.md` first required Framework amendment matching the descriptor latest amendment;
- Direct-to-Latest descriptor policy, preservation flags, path classes, and one final full verification setting;
- README, SKILL, root template, skeleton, mockup, and launcher current-distribution identities;
- `[Project Status]`, `[Project Path]`, and `[Project Upgrade]` across current normative/distribution surfaces;
- literal-bracket and case-insensitive registered-command behavior;
- bounded registered Project-command set;
- exact comparison report vocabulary `UP_TO_DATE | UPGRADE_AVAILABLE | SOURCE_DIVERGENCE | VERIFICATION_REQUIRED`;
- active local `FRAMEWORK-001` pin as current initialized-Project authority;
- fresh canonical upstream as target evidence and rejection of cached/remembered substitutes;
- same-version source divergence and fail-closed unknown/conflicting inputs;
- ask-before-preparation and upgrade-intent approval distinct from mutation approval;
- read-only comparison and no Project Location/branch/runtime authority expansion;
- `FAST_PATH | ASSESSED_PATH | MAJOR_MIGRATION_REQUIRED` and current-truth/history preservation;
- no mandatory intermediate migration execution and non-destructive latest-starter boundary;
- rollback/reversibility, affected verification, one final `RELEASE_FULL`, and promotion/history preservation;
- Pressure Scenarios `136–150` present exactly once with normative support;
- byte-identical ChatGPT/Claude shared marker bodies and launcher size limits;
- mandatory response-close fields retained;
- reserved slots `18–19` retained with no starter files;
- slot `91` ownership retained;
- no executable updater/parser/validator/CLI/CI/scheduler/watcher/runtime artifact in the distribution;
- historical Framework amendments unchanged;
- `git diff --check` clean;
- clean candidate worktree.

The final verification command exited `0`.

## Execution-mode note

The requested Subagent-Driven execution path could not obtain a usable mutating subagent provider in this session: the mutating delegate remained queued, local Codex was absent, and the read-only delegate returned no retrievable result. The approved plan therefore continued inline in the isolated worktree with per-Task commits, task-local checks, review gates, whole-branch review, and fresh final verification. This execution-mode deviation does not change the Framework semantics verified above.

During Task 4, an uncommitted PowerShell encoding attempt produced mojibake. Those uncommitted template changes were fully restored to `HEAD` before reapplying Task 4 with Python UTF-8. A subsequent UTF-8 integrity check passed before commit `d34d323`; no corrupted text from that attempt is present in the final candidate.

## Integration gate status

`INTEGRATION_GATE: NOT_APPLICABLE` for the release-completion checkpoint because no branch/worktree integration is performed by this evidence update. Any later integration of `work/project-upgrade-command` into a canonical target must fresh-resolve the Canonical Integration Target, Base Freshness, and evidence validity before integration.

## Completion interpretation

This evidence establishes a verified final Framework distribution candidate. It does not prove remote publication. `commit ≠ push` remains binding. Task-source completion is valid only when this final evidence revision is durably committed, its commit is observed, the remaining worktree is clean or understood, and `TASK-018` references this final verification truth rather than the superseded `64/64` evidence state.
