# Framework 1.3.1 Project Upgrade Command — RELEASE_FULL Evidence

Captured: `2026-08-24T19:43:04+07:00`

Task: `TASK-018`

Purpose: bind the final Framework `1.3.1` / Schema `1.0.0` Project Upgrade command verification to the observed unchanged Framework distribution candidate before Task-source reconciliation. This evidence records actual checks and observed Git identities; it does not treat commit as push and does not infer remote publication.

## Verified distribution candidate

- Branch: `work/project-upgrade-command`
- Candidate HEAD: `f06e3be7bcce915e528f92284ef11946deffbd9f`
- Candidate tree: `9bec80b8a2cc0b2ece5af9289f0524185ba5578b`
- `managing-project-source/` subtree: `fac5c6f37a5b8f6b1b33bad21b563ecf1c972425`
- `README.md` blob: `cf9d2ab574bde8a36fbdb27fb54d2bb84bceff3b`
- Worktree before `RELEASE_FULL`: clean
- Framework release: `1.3.1`
- Project Source Schema: `1.0.0`
- Release format: `3`
- Latest amendment: `managing-project-source/references/framework-governance-amendment-260824-1708.md`

The evidence file itself is created after the distribution verification. It is not a Framework distribution semantic mutation and therefore is not part of the verified candidate tree above. No Framework distribution file is changed after the `RELEASE_FULL` run unless verification is rerun and rebound.

## Implementation commits in the verified candidate

- `2086e0d` — `test: add project upgrade command pressure scenarios`
- `01f56dd` — `docs: define framework 1.3.1 project upgrade command`
- `8853f43` — `feat: register project upgrade command governance`
- `d34d323` — `docs: propagate framework 1.3.1 project upgrade command`
- `f06e3be` — `docs: expose project upgrade command in launchers`

Task 6 affected verification required no correction commit after these changes.

## Affected verification

Fresh affected verification on the final candidate returned:

`AFFECTED VERIFICATION PASS 56/56`

It covered release identity, current starter metadata, historical-amendment preservation, registered command coverage, bounded command registry, comparison vocabulary, fresh-upstream/current-local-pin separation, upgrade-intent versus mutation approval, Direct-to-Latest path classes/preservation, Scenario 136–150 presence and normative mapping, launcher equality/size, reserved slots, and prohibited artifact scope.

## RELEASE_FULL result

One full current-distribution structural verification was run after the Framework distribution candidate stopped changing.

Result:

`RELEASE_FULL PASS 64/64`

Launcher lengths observed in that run:

- ChatGPT launcher: `4495` Unicode characters
- Claude launcher: `4492` Unicode characters

The 64 passing checks covered:

- Framework `1.3.1` / Schema `1.0.0` / release format `3` identity;
- stable release channel and canonical repository identity;
- latest Framework amendment pointer and approval provenance;
- Direct-to-Latest descriptor policy, preservation flags, path classes, and one final full verification setting;
- README, SKILL, root template, skeleton, mockup, and launcher current-distribution identities;
- `[Project Status]`, `[Project Path]`, and `[Project Upgrade]` across current normative/distribution surfaces;
- literal-bracket and case-insensitive registered-command behavior;
- bounded registered Project-command set;
- exact comparison report vocabulary `UP_TO_DATE | UPGRADE_AVAILABLE | SOURCE_DIVERGENCE | VERIFICATION_REQUIRED`;
- active local `FRAMEWORK-001` pin as current initialized-Project authority;
- fresh canonical upstream as target evidence and rejection of cached/remembered substitutes;
- same-version source divergence and fail-closed unknown/conflicting inputs;
- ask-before-preparation and upgrade-intent approval distinct from mutation approval;
- read-only comparison and no Project Location/branch/runtime authority expansion;
- `FAST_PATH | ASSESSED_PATH | MAJOR_MIGRATION_REQUIRED` and current-truth/history preservation;
- no mandatory intermediate migration execution and non-destructive latest-starter boundary;
- rollback/reversibility, affected verification, one final `RELEASE_FULL`, and promotion/history preservation;
- Pressure Scenarios `136–150` present exactly once with normative support;
- byte-identical ChatGPT/Claude shared marker bodies and launcher size limits;
- mandatory response-close fields retained;
- reserved slots `18–19` retained with no starter files;
- slot `91` ownership retained;
- no executable updater/parser/validator/CLI/CI/scheduler/watcher/runtime artifact in the distribution;
- historical Framework amendments unchanged;
- `git diff --check` clean;
- clean candidate worktree.

The verification command exited `0`.

## Task-level execution note

The requested Subagent-Driven execution path could not obtain a usable mutating subagent provider in this session: the mutating delegate remained queued, local Codex was absent, and the read-only delegate returned no retrievable result. The approved plan therefore continued inline in the isolated worktree with per-Task commits, explicit task-local checks, and separate diff/review gates. This execution-mode deviation does not change the Framework semantics verified above.

During Task 4, an uncommitted PowerShell encoding attempt produced mojibake. Those uncommitted template changes were fully restored to `HEAD` before reapplying the Task 4 edits with Python UTF-8. A subsequent UTF-8 integrity check passed before commit `d34d323`; no corrupted text from that failed attempt is present in the verified candidate.

## Integration gate status

`INTEGRATION_GATE: NOT_APPLICABLE` for this Task 7 execution because no branch/worktree integration is performed by this checkpoint. Any later integration of `work/project-upgrade-command` into a canonical target must fresh-resolve the Canonical Integration Target, Base Freshness, and evidence validity before integration.

## Completion interpretation

This evidence establishes a verified Framework distribution candidate. It does not itself prove remote publication. `commit ≠ push` remains binding. Task-source reconciliation may mark `TASK-018` `DONE` only after this evidence is durably committed, the resulting completion commit(s) are observed, the remaining worktree is clean or understood, and no unresolved Task completion blocker remains.
