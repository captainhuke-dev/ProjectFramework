# UAAC 5.0 Constitution-First / Runtime-Free Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Execution selection for this run:** Inline execution with `superpowers:executing-plans` is already owner-authorized. Do not pause for another execution-choice gate. This session does not delegate to subagents.

**Goal:** Release UAAC 5.0.0 as a locally readable, Constitution-first, runtime-free production distribution while preserving Framework 1.2.5, all 25 stable law IDs, and immutable reconstruction of UAAC 4.2.

**Architecture:** The production package contains only normative law Markdown plus non-normative Markdown/YAML navigation, install, migration, template, profile, and release material. Python, JSON Schema, fixtures, developer registers, and executable verification move to the removable sibling `uaac-conformance/`; installed Projects need only a persistent launcher, a small router, a minimal adoption record, and a locally vendored exact Constitution release.

**Tech Stack:** Markdown, YAML 1.2-compatible data, Git; developer-only Python, pytest, PyYAML, and JSON Schema Draft 2020-12 under `uaac-conformance/`.

**Spec:** `docs/superpowers/specs/2026-08-23-uaac-5.0-constitution-first-runtime-free-design.md`

## Global Constraints

- Repository: `captainhuke-dev/ProjectFramework`; the only authorized push target is `refs/heads/hz-framework`.
- Never mutate or push `main`; never force-push, rewrite history, change another repository, expose secrets, or deploy unrelated systems.
- Candidate ancestry must include observed `origin/main` `d5b27ab9856c7cf6da5c47dbad4ab57534e4f7f9` and observed `origin/hz-framework` `e2463f113ebb4b84119b8f165862812d27a178b8`; reconciled merge is `8ea3700dcf126f926b0d5ad0fad4d1c3669d1b8d`.
- Current Framework semantics remain 1.2.5 and `managing-project-source/**` must remain byte-equivalent to the current `origin/main` version unless a later fresh main observation requires semantic reconciliation.
- UAAC version becomes exactly `5.0.0`; law IDs remain exactly and uniquely `CONST-001` through `CONST-025`.
- Canonical operational law exists only in `universal-ai-agent-constitution/laws/CONST-001.md` through `CONST-025.md`.
- `universal-ai-agent-constitution/` may contain only `.md`, `.yaml`, and `.yml` files and no executable/runtime dependency.
- UAAC Core must remain usable when `uaac-conformance/`, Python, pytest, JSON Schema, OpenViking, Serena, MCP, RAG, vector DB, native Skills, CI, and network access are absent.
- Project rules are conditional; never create an empty rules artifact merely to satisfy an installation shape.
- `UAAC-ADOPTION.yaml` remains limited to Project boundary, Constitution identity/local locator, actual rules/sources, optional continuation, and explicitly adopted profiles.
- Auto-Boot is persistent launcher behavior, not runtime infrastructure.
- Installing UAAC never installs or upgrades ProjectFramework.
- Profiles are opt-in, non-normative, and gain no authority by being present.
- Historical UAAC 4.2 core resolves release commit `5a309d8d38046bf3e8cd4beb2fc82a872f211cad` and package tree `3e62912bcbd88d91339dfa772dc6776ee95c77c5`; the completed reference fixture resolves snapshot commit `5cc9488427c8034a67f4898ace5f1c5806760b85`.
- All repository edits use `apply_patch`; exact tracked removals use `git rm` only after their immutable reconstruction route has a passing guard.
- Values written as PowerShell variables such as `$implementationSha` below are runtime observations that must be populated from command output; they are not design placeholders.

---

## File responsibility map

### Production UAAC

- `universal-ai-agent-constitution/UAAC-v5.0-CONSTITUTION.md`: identity, preamble, precedence, applicability/navigation, version, and law index only.
- `universal-ai-agent-constitution/laws/CONST-001.md` … `CONST-025.md`: the sole operational constitutional requirements.
- `universal-ai-agent-constitution/LAW-MANIFEST.yaml`: non-normative 25-law navigation inventory.
- `universal-ai-agent-constitution/CONSTITUTION-RELEASE.yaml`: non-normative version, boundary, provenance, and accurately scoped assurance metadata.
- `universal-ai-agent-constitution/INSTALL-UAAC.md`: non-executable Greenfield/Brownfield installer protocol.
- `universal-ai-agent-constitution/ADOPTION-GUIDE.md`: interpretation of the exact minimal adoption fields and bounded reading route.
- `universal-ai-agent-constitution/MIGRATION-v4.2-TO-v5.0.md`: side-by-side migration and qualified rollback.
- `universal-ai-agent-constitution/templates/PROJECT-UAAC.md`: small `governance/UAAC.md` router template.
- `universal-ai-agent-constitution/templates/UAAC-ADOPTION.yaml`: minimal strict adoption template.
- `universal-ai-agent-constitution/templates/PROJECT-RULES.md`: authoring aid used only when real rules exist.
- `universal-ai-agent-constitution/templates/platform/*.md`: persistent launcher snippets only.
- `universal-ai-agent-constitution/profiles/*/PROFILE.md`: optional implementation mappings with explicit non-authority.

### Developer conformance

- `uaac-conformance/tools/validate_distribution.py`: read-only validator library and CLI used only by developers.
- `uaac-conformance/template-schema-map.yaml`: exhaustive declaration of production YAML templates that have schemas.
- `uaac-conformance/schemas/uaac-adoption.schema.json`: strict schema for the minimal adoption record.
- `uaac-conformance/tests/test_distribution.py`: extension, layout, version, entrypoint, and release-boundary checks.
- `uaac-conformance/tests/test_laws.py`: exact IDs, sole normative ownership, universal-property vocabulary, and mechanism-leakage checks.
- `uaac-conformance/tests/test_templates.py`: positive validation and independent negative corruption for every declared pair.
- `uaac-conformance/tests/test_installation.py`: Greenfield/Brownfield, offline pin, Project-rules, Auto-Boot, and Framework-separation checks.
- `uaac-conformance/tests/test_profiles.py`: opt-in/non-authority profile checks.
- `uaac-conformance/tests/test_history.py`: immutable v4.2 core and fixture reconstruction checks.
- `uaac-conformance/tests/test_links.py`: local Markdown/YAML locator integrity.
- `uaac-conformance/tests/test_release_claims.py`: release claims cannot exceed executed check definitions.
- `uaac-conformance/CONSTITUTIONAL-ACCEPTANCE.md`: CA-01 through CA-11 as Human/LLM-readable, no-Python scenarios.
- `uaac-conformance/fixtures/{greenfield,brownfield,migration-v4.2}/`: concrete static expected installations/mappings.

### History, navigation, and evidence

- `docs/uaac-history/v4.2/README.md`: immutable reconstruction guide and classification.
- `docs/uaac-history/v4.2/REFERENCE-INVENTORY.yaml`: exact release, tree, reference-snapshot, and relocated-path categories.
- `README.md`, `UAAC.md`, `HUMAN-INSTALL-WALKTHROUGH-TH.md`: current repository-level product separation and v5 navigation.
- `docs/uaac-repair/UAAC-CONSTITUTION-FIRST-PATCH-REPORT.md`: phase-B human audit report.
- `docs/uaac-repair/UAAC-CONSTITUTION-FIRST-PATCH-STATE.yaml`: phase-B machine-readable evidence with `evidence_commit_identity: SELF`.

---

### Task 1: Create the external RED conformance boundary

**Files:**
- Create: `uaac-conformance/README.md`
- Create: `uaac-conformance/requirements-dev.txt`
- Create: `uaac-conformance/tools/validate_distribution.py`
- Create: `uaac-conformance/tests/conftest.py`
- Create: `uaac-conformance/tests/test_distribution.py`
- Create: `uaac-conformance/tests/test_laws.py`
- Create: `uaac-conformance/tests/test_templates.py`
- Create: `uaac-conformance/tests/test_installation.py`
- Create: `uaac-conformance/tests/test_profiles.py`
- Create: `uaac-conformance/tests/test_history.py`
- Create: `uaac-conformance/tests/test_links.py`
- Create: `uaac-conformance/tests/test_release_claims.py`

**Interfaces:**
- Consumes: repository root and current v4.2 package.
- Produces: `load_yaml(path: Path) -> object`, `production_files(root: Path) -> list[Path]`, `law_files(root: Path) -> list[Path]`, `markdown_targets(path: Path) -> list[str]`, `schema_errors(instance: object, schema: object) -> list[str]`, `git_tree(repo_root: Path, commit: str, path: str) -> str`, and CLI `main(argv: Sequence[str] | None = None) -> int`.

- [ ] **Step 1: Add the non-normative boundary declaration and developer dependencies**

```text
NON-NORMATIVE
DEVELOPER-ONLY
NOT INSTALLED INTO PROJECT
NOT REQUIRED FOR UAAC OPERATION
```

`requirements-dev.txt` contains exactly:

```text
jsonschema>=4.18,<5
PyYAML>=6,<7
pytest>=8,<10
```

- [ ] **Step 2: Add the reusable read-only validation helpers**

```python
ALLOWED_PRODUCTION_SUFFIXES = {".md", ".yaml", ".yml"}
LAW_IDS = tuple(f"CONST-{number:03d}" for number in range(1, 26))
V42_RELEASE_COMMIT = "5a309d8d38046bf3e8cd4beb2fc82a872f211cad"
V42_PACKAGE_TREE = "3e62912bcbd88d91339dfa772dc6776ee95c77c5"
V42_REFERENCE_COMMIT = "5cc9488427c8034a67f4898ace5f1c5806760b85"
MARKDOWN_LINK = re.compile(r"\[[^\]]+\]\(([^)]+)\)")

def load_yaml(path: Path) -> object:
    return yaml.safe_load(path.read_text(encoding="utf-8"))

def production_files(root: Path) -> list[Path]:
    return sorted(path for path in root.rglob("*") if path.is_file())

def law_files(root: Path) -> list[Path]:
    return sorted((root / "laws").glob("CONST-*.md"))

def markdown_targets(path: Path) -> list[str]:
    return MARKDOWN_LINK.findall(path.read_text(encoding="utf-8"))

def schema_errors(instance: object, schema: object) -> list[str]:
    validator = jsonschema.Draft202012Validator(schema)
    return sorted(error.message for error in validator.iter_errors(instance))

def git_tree(repo_root: Path, commit: str, path: str) -> str:
    completed = subprocess.run(
        ["git", "rev-parse", f"{commit}:{path}"],
        cwd=repo_root,
        check=True,
        capture_output=True,
        text=True,
    )
    return completed.stdout.strip()
```

The CLI reports every finding, prints `UAAC_CONFORMANCE_PASS` only for zero findings, and returns exit code `0` only in that case.

`conftest.py` exposes concrete `repo_root`, `production_root`, `template_instance`, and `adoption_schema` fixtures. The last two load `universal-ai-agent-constitution/templates/UAAC-ADOPTION.yaml` and `uaac-conformance/schemas/uaac-adoption.schema.json`; their initial missing-file failures are intentional RED results, not collection errors.

- [ ] **Step 3: Write distribution and sole-normative-boundary tests**

```python
def test_production_extensions_are_runtime_free(production_root):
    bad = [p for p in production_files(production_root)
           if p.suffix.lower() not in ALLOWED_PRODUCTION_SUFFIXES]
    assert bad == []

def test_v5_entrypoint_is_present_and_v42_entrypoint_is_absent(production_root):
    assert (production_root / "UAAC-v5.0-CONSTITUTION.md").is_file()
    assert not (production_root / "UAAC-v4.2-CONSTITUTION.md").exists()

def test_entrypoint_has_only_permitted_sections(production_root):
    text = (production_root / "UAAC-v5.0-CONSTITUTION.md").read_text(encoding="utf-8")
    headings = [line for line in text.splitlines() if line.startswith("## ")]
    assert headings == [
        "## Preamble",
        "## Identity and precedence",
        "## Applicability and navigation",
        "## Version and amendment",
        "## Law index",
    ]
```

- [ ] **Step 4: Write exact law-ID and mechanism-leakage tests**

```python
def test_law_ids_are_exact(production_root):
    assert tuple(path.stem for path in law_files(production_root)) == LAW_IDS

FORBIDDEN_NORMATIVE_MECHANISMS = (
    "Boot Receipt", "State Authority Map", "Capability Pack",
    "Adapter Registry", "Skill Registry", "Claim Contract Registry",
    "Document Registry", "Continuation Index", "lineage epoch",
    "LLM Wiki", "JSON Schema", "validator CLI", "ProjectFramework",
)

def test_requirements_do_not_prescribe_old_machinery(production_root):
    for path in law_files(production_root):
        requirements = path.read_text(encoding="utf-8").split("## Compliance", 1)[0]
        assert [term for term in FORBIDDEN_NORMATIVE_MECHANISMS
                if term.lower() in requirements.lower()] == []
```

Also parameterize required concepts for all 25 laws from the exact matrix in Task 2, with especially strict assertions for `CONST-008`, `CONST-022`, and `CONST-024`.

- [ ] **Step 5: Write template, install, profile, history, link, and claim tests before their targets exist**

```python
@pytest.mark.parametrize("forbidden", [
    "runtime_status", "epoch", "boot_receipt", "claim_token",
    "boot_mode", "adapter_registry", "capability_graph", "tool_config",
])
def test_adoption_rejects_runtime_machinery(template_instance, adoption_schema, forbidden):
    candidate = copy.deepcopy(template_instance)
    candidate[forbidden] = {"enabled": True}
    assert schema_errors(candidate, adoption_schema)

def test_install_does_not_require_projectframework(production_root):
    install = (production_root / "INSTALL-UAAC.md").read_text(encoding="utf-8")
    assert "does not install or upgrade ProjectFramework" in install

def test_historical_core_is_reconstructible(repo_root):
    assert git_tree(repo_root, V42_RELEASE_COMMIT, "universal-ai-agent-constitution") == V42_PACKAGE_TREE
```

The template test reads every `pairs[]` entry in `template-schema-map.yaml`, validates the unmodified template, then deep-copies and applies that pair's declared `negative_mutation` so each pair independently proves a failure.

- [ ] **Step 6: Run the new suite and capture the expected RED evidence**

Run:

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests -q -p no:cacheprovider
```

Expected: FAIL because v5 entrypoint/templates/profiles do not exist, v4.2 runtime files remain in production, and mutable historical fixture locators remain. Failures must be requirement failures, not syntax/import errors.

- [ ] **Step 7: Commit the executable specification**

```powershell
git add uaac-conformance
git commit -m "test: define UAAC 5.0 external conformance"
```

### Task 2: Rewrite the Constitution entrypoint and all 25 laws

**Files:**
- Create: `universal-ai-agent-constitution/UAAC-v5.0-CONSTITUTION.md`
- Modify: `universal-ai-agent-constitution/laws/CONST-001.md` … `CONST-025.md`
- Modify: `universal-ai-agent-constitution/LAW-MANIFEST.yaml`
- Modify: `universal-ai-agent-constitution/CONSTITUTION-RELEASE.yaml`
- Modify: `universal-ai-agent-constitution/CHANGELOG.md`
- Test: `uaac-conformance/tests/test_distribution.py`
- Test: `uaac-conformance/tests/test_laws.py`

**Interfaces:**
- Consumes: exact `LAW_IDS` and entrypoint heading contract from Task 1.
- Produces: a five-section non-operational Constitution entrypoint, 25 law files with `## Constitutional requirements`, `## Compliance`, and `## Non-prescription`, and coherent v5 metadata.

- [ ] **Step 1: Strengthen concept assertions for every stable law**

Use this exact required-concept matrix in `test_laws.py`:

| ID | Required concepts |
|---|---|
| `CONST-001` | universal scope; applicable constraints; substrate independence |
| `CONST-002` | identity; responsibility; capability; authority; permission |
| `CONST-003` | human-origin authority; delegation; revocation; accountability |
| `CONST-004` | truthful Agent/entity identity; no impersonation |
| `CONST-005` | Current Truth; uncertainty; conflict; Memory != Current Truth |
| `CONST-006` | proportional evidence; traceability; unverifiable claim limits |
| `CONST-007` | instruction authority; untrusted input; conflict handling |
| `CONST-008` | bounded materially required sources; sufficient complete coverage; routing is not coverage |
| `CONST-009` | recover canonical source before guessing; disclose unavailable source |
| `CONST-010` | risk; authority limit; safe escalation; reversibility |
| `CONST-011` | honest capability; limitation disclosure; no invented access |
| `CONST-012` | decision basis; countercase; uncertainty; change conditions |
| `CONST-013` | semantic fidelity; explicit abridgment; no concealed change |
| `CONST-014` | durable continuation when materially necessary; sufficient recovery |
| `CONST-015` | safe handoff; receiver verification; no authority transfer |
| `CONST-016` | artifact identity; freshness; synchronization; conflict |
| `CONST-017` | execution; verification; acceptance; publication; deployment; closure |
| `CONST-018` | scoped fail-closed; explicit non-compliance; unaffected work |
| `CONST-019` | proportional reproducibility; reconstruction; limits |
| `CONST-020` | tools/frameworks/extensions optional; subordinate; presence is not authority |
| `CONST-021` | minimal adoption; local Constitution; actual routes; explicit profiles |
| `CONST-022` | amendment; version; migration; rollback; source-appropriate immutable/verifiable identity |
| `CONST-023` | reusable procedures optional; automatically considered when available; no native Skill dependency |
| `CONST-024` | retrieval/memory integrity; derived context; non-authority; source verification |
| `CONST-025` | claim != proof; epistemic honesty; evidence-backed status |

- [ ] **Step 2: Run the law tests and verify they fail against v4.2 wording**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_laws.py -q -p no:cacheprovider
```

Expected: FAIL on mechanism leakage and missing v5 universal-property wording.

- [ ] **Step 3: Rewrite laws 001–007 with universal properties only**

Each `## Constitutional requirements` section contains only its matrix concepts and observable duties. `## Compliance` defines compliant/non-compliant outcomes without a mandated artifact format. `## Non-prescription` explicitly leaves filenames, storage, tools, and automation to the adopting Project.

- [ ] **Step 4: Rewrite laws 008–013 with bounded comprehension and source recovery**

`CONST-008` must say all of the following in substance:

```text
Before relying on Project sources, identify a bounded set materially required for the task.
Obtain sufficient complete coverage of every selected source.
Search, relevance ranking, retrieval, and summaries may route reading but cannot by themselves establish complete coverage.
Do not expand reading to every merely applicable source when it is not materially required.
```

- [ ] **Step 5: Rewrite laws 014–019 with proportional continuation, handoff, and status truth**

Do not prescribe indices, lineage, epochs, packets, receipts, hashes, lifecycle engines, or ledgers. Preserve durable continuation only when context loss would materially impair safe continuation, and distinguish execution from every later assurance/publication state.

- [ ] **Step 6: Rewrite laws 020–025 with minimal adoption and source-appropriate identity**

`CONST-022` must permit Git SHAs, content digests, immutable object IDs, signed revisions, and equivalents without making one mechanism universal. `CONST-024` must preserve `Memory != Current Truth` and make OpenViking/RAG/vector infrastructure optional. `CONST-025` must preserve `claim != proof` without tokens or a claim registry.

- [ ] **Step 7: Add the constrained entrypoint and navigation manifest**

The entrypoint uses exactly the five headings asserted in Task 1. `LAW-MANIFEST.yaml` declares `status: NON_NORMATIVE_NAVIGATION`, version `5.0.0`, and one `{id, locator, title}` entry per law; it never claims to prove reading coverage.

- [ ] **Step 8: Update release metadata and changelog without overclaiming verification**

`CONSTITUTION-RELEASE.yaml` declares version, date, 25-law count, production extensions, normative root, and immutable v4.2 predecessor identity. It lists verification definitions but no PASS result until those checks actually run. `CHANGELOG.md` identifies 5.0.0 as a breaking architectural release and preserves concise 4.2 history with immutable reconstruction links.

- [ ] **Step 9: Run the focused law/distribution tests**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_laws.py uaac-conformance\tests\test_distribution.py -q -p no:cacheprovider
```

Expected: law/entrypoint/version assertions PASS; extension/layout assertions may remain RED until legacy runtime files are removed in Task 6.

- [ ] **Step 10: Commit the constitutional core**

```powershell
git add universal-ai-agent-constitution\UAAC-v5.0-CONSTITUTION.md universal-ai-agent-constitution\laws universal-ai-agent-constitution\LAW-MANIFEST.yaml universal-ai-agent-constitution\CONSTITUTION-RELEASE.yaml universal-ai-agent-constitution\CHANGELOG.md uaac-conformance\tests
git commit -m "feat: rewrite UAAC 5.0 constitutional core"
```

### Task 3: Implement minimal adoption, installer, and router templates

**Files:**
- Modify: `universal-ai-agent-constitution/README.md`
- Modify: `universal-ai-agent-constitution/INSTALL-UAAC.md`
- Modify: `universal-ai-agent-constitution/ADOPTION-GUIDE.md`
- Create: `universal-ai-agent-constitution/MIGRATION-v4.2-TO-v5.0.md`
- Create: `universal-ai-agent-constitution/templates/PROJECT-UAAC.md`
- Create: `universal-ai-agent-constitution/templates/UAAC-ADOPTION.yaml`
- Create: `universal-ai-agent-constitution/templates/PROJECT-RULES.md`
- Modify: `universal-ai-agent-constitution/templates/platform/AGENTS.md`
- Modify: `universal-ai-agent-constitution/templates/platform/CHATGPT-PROJECT-INSTRUCTIONS.md`
- Modify: `universal-ai-agent-constitution/templates/platform/GENERIC-AGENT-INSTRUCTIONS.md`
- Create: `uaac-conformance/schemas/uaac-adoption.schema.json`
- Create: `uaac-conformance/template-schema-map.yaml`
- Test: `uaac-conformance/tests/test_templates.py`
- Test: `uaac-conformance/tests/test_installation.py`

**Interfaces:**
- Consumes: `UAAC-001@5.0.0`, the local v5 entrypoint, and law IDs 005, 007, 008, 009, 014, 020, 021, 022, 023, and 024.
- Produces: strict adoption object with required keys `project` and `constitution`, optional keys `project_rules`, `canonical_sources`, `continuation`, and `profiles`, and no other top-level key.

- [ ] **Step 1: Complete strict schema tests before the schema exists**

The valid instance is:

```yaml
project:
  id: fixture-greenfield
  boundary: .
constitution:
  id: UAAC-001
  version: "5.0.0"
  local_locator: vendor/uaac/v5.0.0/UAAC-v5.0-CONSTITUTION.md
  immutable_identity: fixture-object:uaac-v5-greenfield-r1
```

Assert optional sections are omitted in the canonical template, root and nested `additionalProperties` are false, lists require at least one real item, locators are non-empty strings, and every forbidden runtime field independently fails.

- [ ] **Step 2: Run template/install tests and verify RED**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_templates.py uaac-conformance\tests\test_installation.py -q -p no:cacheprovider
```

Expected: FAIL because v5 template/schema and runtime-free installer are absent.

- [ ] **Step 3: Write the exact minimal template and Draft 2020-12 schema**

The template contains only the six required fields shown above, using documented install-time metavariables for Project identity, boundary, local locator, and immutable identity. Optional examples live in comments in `ADOPTION-GUIDE.md`, not as empty template objects.

`template-schema-map.yaml` contains one exhaustive pair:

```yaml
document_type: UAAC_TEMPLATE_SCHEMA_MAP
status: DEVELOPER_ONLY_NON_NORMATIVE
pairs:
  - id: uaac-adoption
    template: universal-ai-agent-constitution/templates/UAAC-ADOPTION.yaml
    schema: uaac-conformance/schemas/uaac-adoption.schema.json
    negative_mutation:
      operation: add_root_key
      key: runtime_status
      value: active
```

- [ ] **Step 4: Write the small non-authoritative Project router**

`PROJECT-UAAC.md` routes adoption → locally pinned Constitution → bounded materially required laws/rules/sources → optional continuation → work. It cites controlling law IDs, preserves persistent Auto-Boot, and explicitly denies independent authority, mutable state, runtime tooling, registries, receipts, remote-only Constitution access, and ProjectFramework installation.

- [ ] **Step 5: Write persistent platform launchers**

Each launcher contains only this semantic instruction:

```text
Before material work, read governance/UAAC.md and follow its local routes without requiring the user to restate UAAC, name a Skill, or request a Wiki read. The launcher creates no independent authority.
```

- [ ] **Step 6: Rewrite Greenfield and Brownfield installation flows**

The installer requires inspection, exact local vendoring, minimal adoption, conditional real rules, direct actual source routes, a small router, non-destructive launcher reconciliation, fresh readback, and no-Python constitutional acceptance. It says verbatim: `Installing UAAC does not install or upgrade ProjectFramework.` It contains no mandatory validator, CLI, receipt, registry, Wiki, Skill, Python, or network step.

- [ ] **Step 7: Write side-by-side v4.2 migration and qualified rollback**

The migration guide records the old pin and routes, vendors v5 beside v4.2, builds and verifies the v5 route before launcher switch, maps old machinery to minimal semantics, archives only after verification/authority, and explains why rollback after v5 state changes requires compatibility assessment and possible remapping.

- [ ] **Step 8: Run focused tests**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_templates.py uaac-conformance\tests\test_installation.py -q -p no:cacheprovider
```

Expected: PASS.

- [ ] **Step 9: Commit the runtime-free adoption path**

```powershell
git add universal-ai-agent-constitution uaac-conformance\schemas uaac-conformance\template-schema-map.yaml uaac-conformance\tests
git commit -m "feat: add minimal runtime-free UAAC adoption"
```

### Task 4: Add opt-in profiles without constitutional authority

**Files:**
- Create: `universal-ai-agent-constitution/profiles/high-assurance/PROFILE.md`
- Modify: `universal-ai-agent-constitution/profiles/humanizer/PROFILE.md`
- Modify: `universal-ai-agent-constitution/profiles/openviking/PROFILE.md`
- Modify: `universal-ai-agent-constitution/profiles/projectframework/PROFILE.md`
- Create: `universal-ai-agent-constitution/profiles/reusable-procedures/PROFILE.md`
- Test: `uaac-conformance/tests/test_profiles.py`

**Interfaces:**
- Consumes: explicit adoption `profiles[]` entries.
- Produces: exactly five discoverable profile IDs whose presence does not activate them.

- [ ] **Step 1: Write profile contract tests**

```python
PROFILE_IDS = {
    "high-assurance", "humanizer", "openviking",
    "projectframework", "reusable-procedures",
}

def test_profiles_are_opt_in_and_non_authoritative(production_root):
    for profile_id in PROFILE_IDS:
        text = (production_root / "profiles" / profile_id / "PROFILE.md").read_text(encoding="utf-8")
        assert "OPTIONAL" in text
        assert "NON-NORMATIVE" in text
        assert "presence does not activate" in text.lower()
        assert "cannot create authority" in text.lower()
```

- [ ] **Step 2: Run and observe RED for missing/legacy profile contracts**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_profiles.py -q -p no:cacheprovider
```

- [ ] **Step 3: Write bounded mappings**

High-assurance may map signed identities, registers, receipts, schemas/CI, richer handoff, and state maps; OpenViking may map retrieval/memory; ProjectFramework maps only an already separately adopted version; reusable-procedures maps native Skills or alternatives; humanizer may change presentation but not meaning. Every profile denies activation-by-presence and authority creation.

- [ ] **Step 4: Run and commit**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_profiles.py -q -p no:cacheprovider
git add universal-ai-agent-constitution\profiles uaac-conformance\tests\test_profiles.py
git commit -m "feat: define optional UAAC assurance profiles"
```

### Task 5: Add no-Python acceptance and concrete install fixtures

**Files:**
- Create: `uaac-conformance/CONSTITUTIONAL-ACCEPTANCE.md`
- Create: `uaac-conformance/fixtures/greenfield/AGENTS.md`
- Create: `uaac-conformance/fixtures/greenfield/governance/UAAC.md`
- Create: `uaac-conformance/fixtures/greenfield/governance/UAAC-ADOPTION.yaml`
- Create: `uaac-conformance/fixtures/brownfield/AGENTS.md`
- Create: `uaac-conformance/fixtures/brownfield/governance/UAAC.md`
- Create: `uaac-conformance/fixtures/brownfield/governance/UAAC-ADOPTION.yaml`
- Create: `uaac-conformance/fixtures/brownfield/governance/PROJECT-RULES.md`
- Create: `uaac-conformance/fixtures/brownfield/project-docs/PRD.md`
- Create: `uaac-conformance/fixtures/brownfield/project-docs/CURRENT-STATE.md`
- Create: `uaac-conformance/fixtures/migration-v4.2/MIGRATION-MAP.yaml`
- Create: `uaac-conformance/fixtures/migration-v4.2/ROLLBACK-NOTES.md`
- Test: `uaac-conformance/tests/test_installation.py`

**Interfaces:**
- Consumes: production templates, v5 laws, and v4.2 mapping.
- Produces: CA-01 through CA-11 and three static fixtures with no runtime status claims.

- [ ] **Step 1: Add failing fixture/acceptance assertions**

Assert IDs are exactly `CA-01` … `CA-11`; each scenario has `Given`, `When`, `Then`, `Required reading`, and `No executable prerequisite`; Greenfield omits Project rules and continuation; Brownfield points at actual existing rules/PRD/state; migration retains both old immutable pin and v5 local route plus a qualified rollback note.

- [ ] **Step 2: Write CA-01 through CA-11 as readback scenarios**

Use the exact acceptance conditions from Design §19: offline Constitution, ordinary-task Auto-Boot, bounded complete coverage, canonical source over memory, conformance deletion, no empty Greenfield artifacts, Brownfield reuse, material continuation, no ProjectFramework installation, safe v4.2 migration/rollback, and law-file normative resolution.

- [ ] **Step 3: Write concrete fixtures**

Greenfield adoption has only required keys. Brownfield adoption adds real `project_rules`, two `canonical_sources` roles, and real `continuation`; it does not duplicate their contents. Migration map explicitly maps Binding, Kernel, Law/Registry, state map, continuation, Skills/adapters, claims, Wiki, and validation to v5 routes or optional profiles.

- [ ] **Step 4: Verify acceptance is executable-independent**

Read `uaac-conformance/CONSTITUTIONAL-ACCEPTANCE.md` directly and confirm no step begins with `Run`, `python`, `pytest`, a CLI command, or a network requirement. Then run only the developer parser test:

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_installation.py -q -p no:cacheprovider
```

- [ ] **Step 5: Commit**

```powershell
git add uaac-conformance\CONSTITUTIONAL-ACCEPTANCE.md uaac-conformance\fixtures uaac-conformance\tests\test_installation.py
git commit -m "test: add runtime-free UAAC acceptance fixtures"
```

### Task 6: Freeze v4.2 reconstruction and remove old machinery from production

**Files:**
- Create: `docs/uaac-history/v4.2/README.md`
- Create: `docs/uaac-history/v4.2/REFERENCE-INVENTORY.yaml`
- Modify: mutable locators under `uaac-v4.2-reference-project/`
- Remove from production: `examples/`, `registers/`, `reviews/`, `schemas/`, `skills/`, `tests/`, `tools/`, `validation/`
- Remove superseded production files: `ADOPTION-RUNBOOK.md`, `HUMAN-INSTALL-WALKTHROUGH-TH.md`, `INSTALLATION-THREAT-MODEL.md`, `PUBLICATION-CONTRACT.yaml`, `SKILL.md`, `SYSTEMS-THINKING-ANALYSIS-TH.md`, `V3-TO-V4-TRACEABILITY.md`, `V4.0-TO-V4.1-TRACEABILITY.md`, `V4.1-TO-V4.2-TRACEABILITY.md`, `UAAC-v4.2-CONSTITUTION.md`
- Remove legacy templates: `AGENT-ADAPTER-REGISTRY.template.yaml`, `AGENTS-UAAC-BOOTSTRAP.md`, `BOOT-RECEIPT.template.yaml`, `BOOTSTRAP-KERNEL.md`, `CHATGPT-PROJECT-INSTRUCTIONS-SHORT.md`, `CHECKPOINT.template.yaml`, `CLAIM-CONTRACT-REGISTRY.template.yaml`, `CONSTITUTION-ADOPTION.template.yaml`, `CONTEXT-BINDING.template.yaml`, `CONTINUATION-POINTER.template.yaml`, `CURRENT-CONTINUATION.template.yaml`, `HANDOFF.template.yaml`, `INSTALLATION-VALIDATION.template.yaml`, `PROJECT-BINDING.template.yaml`, `PROJECT-CAPABILITY-PACK.template.yaml`, `PROJECT-DOCUMENT-REGISTRY.template.yaml`, `PROJECT-GOVERNANCE-README.template.md`, `PROJECT-LLM-WIKI-index.template.md`, `PROJECT-UAAC-BOOT.template.md`, `RESULT.template.yaml`, `SKILL-REGISTRY.template.yaml`, `STATE-AUTHORITY-MAP.template.yaml`, and `SYSTEM-HEALTH-METRICS.template.yaml`.
- Remove: `universal-ai-agent-constitution/profiles/openviking/SOURCE-LOCK.template.yaml`
- Test: `uaac-conformance/tests/test_history.py`
- Test: `uaac-conformance/tests/test_distribution.py`

**Interfaces:**
- Consumes: immutable Git identities and passing history guard.
- Produces: exact production layout and a reference Project that can never silently resolve current v5 bytes.

- [ ] **Step 1: Make the immutable Git-object guard pass before any removal**

The test executes:

```powershell
git cat-file -e 5a309d8d38046bf3e8cd4beb2fc82a872f211cad^{commit}
git rev-parse 5a309d8d38046bf3e8cd4beb2fc82a872f211cad:universal-ai-agent-constitution
git cat-file -e 5cc9488427c8034a67f4898ace5f1c5806760b85^{commit}
```

Expected package tree is exactly `3e62912bcbd88d91339dfa772dc6776ee95c77c5`, and the completed fixture snapshot contains 27 files.

- [ ] **Step 2: Inventory all historical categories and mutable routes**

`REFERENCE-INVENTORY.yaml` records the three identities above and categories for runbooks, schemas, validators, fixtures/scenarios, threat/system reviews, registers, Skills, templates, and validation output. It states that bytes are reconstructed from immutable Git, not from current package paths.

- [ ] **Step 3: Pin the v4.2 reference Project routes**

Replace branch-resolving `blob/hz-framework`, `ref: hz-framework`, `canonical_ref_policy: hz-framework`, and bare current-package resolution with either internal fixture paths or commit-qualified locators. Core UAAC always uses release commit `5a309d8...`; the full reference fixture remote view uses `5cc9488...`.

- [ ] **Step 4: Run history tests and require PASS before deletion**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_history.py -q -p no:cacheprovider
```

- [ ] **Step 5: Remove exact tracked legacy paths**

Use `git rm` only for the exact paths listed in this task. Preserve their Git history and reconstruction documentation. Recreate/retain only the v5 files named in the production map.

- [ ] **Step 6: Verify the production boundary and links**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_distribution.py uaac-conformance\tests\test_history.py uaac-conformance\tests\test_links.py -q -p no:cacheprovider
```

Expected: PASS and no file extension outside `.md`, `.yaml`, `.yml` under production.

- [ ] **Step 7: Commit the boundary split**

```powershell
git add -A -- universal-ai-agent-constitution uaac-v4.2-reference-project docs\uaac-history uaac-conformance
git commit -m "refactor: separate UAAC production from conformance"
```

### Task 7: Reconcile root navigation while preserving Framework 1.2.5

**Files:**
- Modify: `README.md`
- Modify: `UAAC.md`
- Modify: `HUMAN-INSTALL-WALKTHROUGH-TH.md`
- Test: `uaac-conformance/tests/test_installation.py`
- Test: `uaac-conformance/tests/test_links.py`
- Test: `uaac-conformance/tests/test_distribution.py`

**Interfaces:**
- Consumes: final v5 production paths and unchanged Framework distribution.
- Produces: current two-product navigation with no v4.2-as-current or coupled-install language.

- [ ] **Step 1: Add root-navigation assertions**

Assert root README reports Framework `1.2.5`, UAAC `5.0.0`, distinct install actions, current v5 links, historical v4.2 classification, and developer-only conformance. Assert `UAAC.md` routes to the local v5 Constitution, installer, migration, profiles, conformance, and immutable v4.2 history.

- [ ] **Step 2: Rewrite the root README and UAAC navigation**

Use five explicit boundaries: Framework production, UAAC production, optional UAAC profiles, developer-only conformance, and historical/reference v4.2. Retain `Install UAAC != install or upgrade ProjectFramework` and remove all v4.2 boot/registry machinery from current quick-install instructions.

- [ ] **Step 3: Rewrite the Thai human walkthrough for v5**

Keep it non-normative and human-only. Change the installed path to persistent launcher → `governance/UAAC.md` → minimal adoption → local v5 Constitution → actual bounded Project sources. Make Project rules conditional and remove mandatory GitHub, Python, Wiki, registries, receipts, Skills, and ProjectFramework installation.

- [ ] **Step 4: Prove Framework bytes remain controlled by main**

```powershell
git diff --exit-code d5b27ab9856c7cf6da5c47dbad4ab57534e4f7f9 -- managing-project-source
rg -n "framework_version: 1\.2\.5|version: 1\.2\.5" managing-project-source
```

Expected: no Framework diff and observed 1.2.5 identity.

- [ ] **Step 5: Run root/link checks and commit**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests\test_installation.py uaac-conformance\tests\test_links.py uaac-conformance\tests\test_distribution.py -q -p no:cacheprovider
git add README.md UAAC.md HUMAN-INSTALL-WALKTHROUGH-TH.md uaac-conformance\tests
git commit -m "docs: publish UAAC 5.0 repository navigation"
```

### Task 8: Complete developer validation and full local verification

**Files:**
- Verify: `uaac-conformance/tools/validate_distribution.py`
- Verify: `uaac-conformance/tests/*.py`
- Verify: `universal-ai-agent-constitution/**`, root navigation, history, and fixtures.

There are no pre-authorized speculative edits in this task. If verification exposes a defect, first add or identify the exact failing assertion, name the exact affected file, patch only that file, and rerun the focused test before the full suite.

**Interfaces:**
- Consumes: complete candidate tree.
- Produces: a coherent phase-A implementation head with no uncommitted files and evidence for every acceptance field.

- [ ] **Step 1: Read the entire diff and scan for unresolved or stale terms**

```powershell
git diff --stat origin/main...HEAD
git diff --check origin/main...HEAD
rg -n "TBD|TODO|FIXME|UAAC-v4\.2-CONSTITUTION|Python.*required|install.*ProjectFramework|blob/hz-framework" README.md UAAC.md HUMAN-INSTALL-WALKTHROUGH-TH.md universal-ai-agent-constitution uaac-conformance uaac-v4.2-reference-project docs\uaac-history
```

Classify legitimate historical/negative mentions; patch every stale operational route or unresolved marker.

- [ ] **Step 2: Run the developer validator CLI**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B uaac-conformance\tools\validate_distribution.py --repository-root .
```

Expected: `UAAC_CONFORMANCE_PASS` and exit `0`.

- [ ] **Step 3: Run the full developer conformance suite**

```powershell
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests -q -p no:cacheprovider
```

Expected: all tests PASS, including positive and per-pair corruption tests.

- [ ] **Step 4: Perform the no-Python constitutional readback**

Read `uaac-conformance/CONSTITUTIONAL-ACCEPTANCE.md` and each fixture directly. Record CA-01 through CA-11 as PASS only when every Given/When/Then route resolves from Markdown/YAML without invoking Python or network access.

- [ ] **Step 5: Verify deletion independence**

Use an isolated temporary copy made outside the candidate worktree, remove only its copied `uaac-conformance/`, and inspect launcher → router → adoption → local Constitution → laws. Do not modify the candidate. Expected: all operational routes remain inside production/fixture Markdown/YAML.

- [ ] **Step 6: Verify ancestry, Framework, distribution, and cleanliness**

```powershell
git merge-base --is-ancestor d5b27ab9856c7cf6da5c47dbad4ab57534e4f7f9 HEAD
git merge-base --is-ancestor e2463f113ebb4b84119b8f165862812d27a178b8 HEAD
git diff --exit-code d5b27ab9856c7cf6da5c47dbad4ab57534e4f7f9 -- managing-project-source
git status --short
```

Expected: both ancestry checks `0`, no Framework diff, and clean status after the final corrective commit.

- [ ] **Step 7: Commit any final verification-driven correction**

```powershell
git add -A
git commit -m "fix: close UAAC 5.0 conformance gaps"
```

Skip this commit only if `git status --short` is empty; never create an empty commit.

### Task 9: Phase A fast-forward publication and implementation readback

**Files:** none unless reconciliation is required by fresh remote movement.

**Interfaces:**
- Consumes: clean verified candidate.
- Produces: exact `$implementationSha` published at and read back from `refs/heads/hz-framework`.

- [ ] **Step 1: Fresh-fetch both authorized comparison refs**

```powershell
git fetch origin main hz-framework
$finalObservedMain = git rev-parse origin/main
$prePushHz = git rev-parse origin/hz-framework
$implementationSha = git rev-parse HEAD
```

- [ ] **Step 2: Reconcile movement without rewriting history**

If `$finalObservedMain` differs from the starting main, inspect its semantic diff and merge `origin/main`; if `$prePushHz` differs from `e2463f...`, merge `origin/hz-framework`. Resolve conflicts by the ownership rules, rerun Task 8 in full, and reset `$implementationSha` from the resulting HEAD. Never force-push.

- [ ] **Step 3: Require current remote ancestry**

```powershell
git merge-base --is-ancestor $prePushHz HEAD
if ($LASTEXITCODE -ne 0) { throw "Candidate does not descend from current origin/hz-framework" }
git merge-base --is-ancestor $finalObservedMain HEAD
if ($LASTEXITCODE -ne 0) { throw "Candidate does not descend from current origin/main" }
```

- [ ] **Step 4: Push exactly one authorized refspec**

```powershell
git push origin HEAD:refs/heads/hz-framework
```

- [ ] **Step 5: Fetch and read back the implementation SHA and key files**

```powershell
git fetch origin hz-framework
$implementationRemoteReadbackSha = git rev-parse origin/hz-framework
if ($implementationRemoteReadbackSha -ne $implementationSha) { throw "Implementation readback mismatch" }
git show "$implementationRemoteReadbackSha`:universal-ai-agent-constitution/UAAC-v5.0-CONSTITUTION.md"
git show "$implementationRemoteReadbackSha`:managing-project-source/FRAMEWORK-RELEASE.yaml"
```

Expected: exact SHA equality, UAAC 5.0.0, and Framework 1.2.5.

### Task 10: Phase B evidence commit, final push, and final readback

**Files:**
- Create: `docs/uaac-repair/UAAC-CONSTITUTION-FIRST-PATCH-REPORT.md`
- Create: `docs/uaac-repair/UAAC-CONSTITUTION-FIRST-PATCH-STATE.yaml`

**Interfaces:**
- Consumes: literal `$implementationSha`, `$implementationRemoteReadbackSha`, `$finalObservedMain`, test outputs, migration/rollback observations, and known limitations.
- Produces: audit artifacts whose self-identity is `SELF`, then literal `$evidenceCommitSha` and `$finalRemoteHzSha` established externally.

- [ ] **Step 1: Write the human report from observed evidence only**

Include starting/final refs, reconciled ancestry, confirmed and additional defects, rejected suspicions/false alarms, architecture decisions, law-by-law changes, moved/removed files, tooling relocation, installer/Auto-Boot behavior, memory/OpenViking boundaries, migration/rollback, ProjectFramework preservation, every verification command/result, Phase-A SHA equality, limitations, risks, exact next action, and zero/factual blockers.

- [ ] **Step 2: Write strict machine-readable state**

Use this exact self-reference rule:

```yaml
document_type: UAAC_CONSTITUTION_FIRST_PATCH_STATE
status: VERIFIED_IMPLEMENTATION_REMOTE_READBACK
evidence_commit_identity: SELF
implementation_commit_sha: runtime-observed-phase-a-head
implementation_remote_readback_sha: runtime-observed-origin-hz-head-after-phase-a
evidence_commit_remote_readback: EXTERNAL_POST_COMMIT_CHECK_REQUIRED
```

Replace the two `runtime-observed-*` values with the literal equal SHAs captured in Task 9. Do not put a predicted evidence commit SHA inside its own content.

- [ ] **Step 3: Validate evidence accuracy and commit**

```powershell
git diff --check
C:\hzframework\.uaac-venv\Scripts\python.exe -B -m pytest uaac-conformance\tests -q -p no:cacheprovider
git add docs\uaac-repair\UAAC-CONSTITUTION-FIRST-PATCH-REPORT.md docs\uaac-repair\UAAC-CONSTITUTION-FIRST-PATCH-STATE.yaml
git commit -m "docs: record UAAC 5.0 patch evidence"
$evidenceCommitSha = git rev-parse HEAD
```

- [ ] **Step 4: Fresh-fetch and protect the Phase-A remote observation**

```powershell
git fetch origin main hz-framework
$phaseBPrePushHz = git rev-parse origin/hz-framework
if ($phaseBPrePushHz -ne $implementationRemoteReadbackSha) { throw "hz-framework moved after Phase A; reconcile and reverify before push" }
git merge-base --is-ancestor $phaseBPrePushHz HEAD
if ($LASTEXITCODE -ne 0) { throw "Evidence candidate is not a fast-forward" }
```

- [ ] **Step 5: Push Phase B and perform final remote readback**

```powershell
git push origin HEAD:refs/heads/hz-framework
git fetch origin hz-framework
$finalRemoteHzSha = git rev-parse origin/hz-framework
if ($finalRemoteHzSha -ne $evidenceCommitSha) { throw "Final remote readback mismatch" }
git show "$finalRemoteHzSha`:docs/uaac-repair/UAAC-CONSTITUTION-FIRST-PATCH-STATE.yaml"
git show "$finalRemoteHzSha`:universal-ai-agent-constitution/CONSTITUTION-RELEASE.yaml"
git show "$finalRemoteHzSha`:README.md"
```

- [ ] **Step 6: Return the exact owner-required terminal report**

Populate every field from authorization §36, distinguish `FAIL`, `NOT_RUN`, limitations, risks, blockers, and next action, and include canonical GitHub URLs for the Constitution, installer, migration guide, design spec, implementation plan, Patch Report, and Patch State. Report the literal evidence SHA only from final readback.

---

## Plan self-review checklist

- [x] Every Design §1–25 requirement maps to at least one task above.
- [x] All 25 stable law IDs appear in the test and implementation matrix.
- [x] Greenfield, Brownfield, migration, rollback, profiles, no-Python acceptance, developer conformance, Git publication, and remote readback each have explicit steps.
- [x] Historical removal cannot occur before the immutable v4.2 guard passes.
- [x] No task introduces runtime state into adoption or production UAAC.
- [x] No task changes ProjectFramework or couples its installation to UAAC.
- [x] No unresolved `TBD`, `TODO`, `FIXME`, undefined function, file, field, or publication target remains.
- [x] Dynamic Git identities are captured from commands and never guessed or self-embedded.
