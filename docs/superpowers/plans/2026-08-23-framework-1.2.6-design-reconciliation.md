# Framework 1.2.6 Design-Line Reconciliation

Status: RECONCILED_LOCAL_CANDIDATE — NOT PUBLISHED

Reconciliation authority: USER_EXPLICIT_REQUEST — 2026-08-23

Repository: `captainhuke-dev/ProjectFramework`

Common base: `d5b27ab9856c7cf6da5c47dbad4ab57534e4f7f9` (Framework `1.2.5`)

Source line A — later written-spec line:
- `aa95a70c56eb6e4604eee25d6d825ea906c54a9a`
- sequence: `72ce6cc` design Revision 2 → `86527ba` storage-state fix → `0ff784e` written-spec approval → `aa95a70` implementation plan
- latest source commit: 2026-08-22T22:50:14+07:00

Source line B — broader File Storage line:
- `2a3ab0b3ec2c57340ce1834b2ddd94da20a5d0b0`
- sequence: `f5fcb3c` design approval → `88bbab2` File Storage revision → `2a3ab0b` implementation plan
- latest source commit: 2026-08-22T20:49:08+07:00

Reconciliation branch: `framework-1.2.6-design-reconciled`

## Reconciliation Rule

Preserve both Git histories and all non-conflicting approved work. Where semantics conflict, the later explicit written-spec approval on source line A governs. Source line B remains authoritative evidence for richer non-conflicting design detail.

No predecessor branch is reset, rebased, force-updated, deleted, or rewritten by this reconciliation.

## Preserved from `aa95a70`

- `WRITTEN_SPEC_APPROVED` state after explicit storage-state review/fix.
- File Storage binding states remain exactly `BOUND | NOT_APPLICABLE | VERIFICATION_REQUIRED`.
- Verification status remains exactly `VERIFIED | USER_CONFIRMED | VERIFICATION_REQUIRED`.
- `BOUND` pairs only with `VERIFIED` or `USER_CONFIRMED`, never `verification_status: VERIFICATION_REQUIRED`.
- A Project with no external/non-repository storage may omit generic storage entries; do not synthesize `NOT_APPLICABLE` entries merely to populate a template.
- Known-applicable unresolved storage is `VERIFICATION_REQUIRED`; absent/unresolved storage never authorizes fallback to recent/search-ranked targets.
- Framework `1.2.6` keeps Google Drive in the dedicated `project_location_binding.google_drive` Root Governance representation; bootstrap Google Drive locators map to that binding and generic storage must not duplicate Drive authority.
- Pressure Scenarios `87–106` remain intact, contiguous, unique, and before `GREEN Run Instructions`.
- The predecessor implementation plan `docs/superpowers/plans/2026-08-22-framework-1.2.6-bootstrap-location-semantics.md` is preserved unchanged as historical planning evidence.

## Preserved from `2a3ab0b`

- Explicit distinction between Bootstrap Location locators and governed `File Storage Binding`.
- Generalized non-Drive File Storage support for S3/object storage, NAS, SMB/NFS, SharePoint, file servers, durable filesystem storage, and comparable providers.
- Multi-storage content-scope ownership: one governed content scope has one declared authoritative owner at a time.
- Provider-appropriate durable identity and protection against similarly named storage targets.
- Mount/cache/sync paths remain routing/mapping evidence and do not transfer Local Workspace or implementation authority.
- File Storage remains distinct from Canonical Implementation Source and Runtime/Data/Persistent-State authority.
- Actual storage credentials are prohibited; existing `SECRET-*` reference semantics remain the credential-routing mechanism.
- Benign mapping differences remain distinct from material Project-routing mismatch.
- Reusable `templates/project-location-bootstrap.md` distribution artifact is retained in implementation scope.
- `60-Deployment-Plan.template.md` storage/runtime separation is retained in implementation scope.
- State-bound `RELEASE_FULL` evidence checkpoint and explicit `INTEGRATION_GATE` workflow are retained.

## Canonical Reconciled Decisions

1. Six bootstrap/execution concepts remain distinct: Framework Source, Remote Location, File Storage Location, MCP Location, Local Workspace, and dynamically observed current branch/worktree.
2. Valid active local `FRAMEWORK-001` remains initialized-Project authority after resolution; bootstrap locators never silently supersede it.
3. Generic governed root storage is `project_location_binding.file_storage_locations` for non-Google-Drive external storage.
4. Google Drive remains canonical in the dedicated `project_location_binding.google_drive` block for Framework `1.2.6`.
5. Bootstrap `GOOGLE_DRIVE` File Storage Location is a locator that maps to the dedicated Drive binding; duplicate dedicated+generic Drive authority for one content scope is invalid.
6. Generic non-Drive provider vocabulary may include `S3 | NAS | SMB | NFS | SHAREPOINT | OBJECT_STORAGE | FILE_SERVER | FILESYSTEM | OTHER`.
7. Existing approved Scenarios `87–106` are not replaced. The richer implementation plan may append only uncovered cases after the actual last scenario, expected from `107` on this baseline.
8. Project Source Schema remains `1.0.0`; no semantic slot, Stable-ID family, branch-authority field, lifecycle/freshness state, or executable enforcement scope is introduced.

## Canonical Implementation Plan

Use:

`docs/superpowers/plans/2026-08-22-framework-1.2.6-bootstrap-location-file-storage.md`

This richer plan is canonical only after the reconciliation edits in this branch. It incorporates the later written-spec constraints above while retaining the broader non-conflicting coverage from source line B.

The older `...bootstrap-location-semantics.md` plan remains preserved as predecessor evidence and is not the execution plan for the reconciled base.

## Canonical Implementation Base

The canonical implementation base is the resulting two-parent reconciliation merge commit on `framework-1.2.6-design-reconciled`, provided post-merge verification proves:

- both `aa95a70` and `2a3ab0b` are ancestors;
- the worktree is clean;
- no unresolved merge markers remain;
- both implementation-plan artifacts exist;
- approved Scenarios `87–106` exist exactly once and precede `GREEN Run Instructions`;
- the reconciled spec and canonical plan contain the storage-state, dedicated Drive, content-scope, secret, mapping, runtime-separation, and dynamic-Git invariants above.

Implementation branch to create later from that exact verified merge commit:

`framework-1.2.6-implementation`

This reconciliation does not push, open a PR, merge to `main`, or start Framework `1.2.6` normative implementation.
