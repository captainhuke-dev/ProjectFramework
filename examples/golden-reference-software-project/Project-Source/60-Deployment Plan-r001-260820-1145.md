---
project_uuid: "12000000-0000-4000-8000-000000000001"
project_id: "GOLDEN-SW-001"
project_name: "HarborDesk Reference Service"
document_id: "DEPLOY-001"
document_type: "DEPLOYMENT_PLAN"
semantic_slot: "60"
revision: 1
document_status: "ACTIVE"
inherits_from: ["FRAMEWORK-001"]
created_at: "2026-08-20T11:45:00+07:00"
updated_at: "2026-08-20T11:45:00+07:00"
created_by: "ACTOR-002"
created_by_instance: "INST-GOLDEN-001"
epistemic_status: "VERIFIED"
freshness_class: "CHANGEABLE"
project_source_framework_version: "1.2.0"
project_source_schema_version: "1.0.0"
synthetic_reference: true
---

# 60 — Deployment Plan

> **Synthetic / Documentation-only:** No concrete install command is provided because this is a synthetic documentation-only reference with no executable application or container artifact.

## Deployment Scope

Demonstrate Framework `1.2.0` Source and Docker installation/operations contracts for the fictional HarborDesk blueprint without creating or executing software.

## Deployment Support

```text
SOURCE_AND_DOCKER
```

## Common Prerequisites

```text
Host platform responsibility: 64-bit Linux
Database contract: PostgreSQL 16
Configuration semantics: APP_ENV, APP_PORT, DATABASE_URL, DB_PASSWORD
Secret policy: actual credential outside Project Source; SECRET-001 metadata only
Persistent storage: required for PostgreSQL data
Authority: this reference authorizes documentation maintenance only; no production mutation
```

Resource sizing, OS package names, repository URL, package-manager commands, and real artifacts are `UNKNOWN / NOT_APPLICABLE_TO_SYNTHETIC_REFERENCE` rather than invented.

## Source Installation View

1. **Source Acquisition Responsibility:** A real implementation would identify its authoritative source repository/artifact. No HarborDesk source exists here.
2. **Runtime Responsibility:** A real Source deployment would provide Python 3.12 and the application dependencies corresponding to the declared Tech Stack.
3. **Dependency Installation Responsibility:** Exact dependency/package commands belong to the real implementation and are not invented in this reference.
4. **Configuration Preparation:** Provide APP_ENV and APP_PORT semantics plus externally resolved database connection/credential inputs.
5. **Secret Resolution:** Resolve actual database secret outside Project Source using the external store represented by `SECRET-001`.
6. **Database / Storage Initialization:** Prepare a PostgreSQL 16 service and durable storage under the real environment's authority/process.
7. **Application Initialization:** Apply implementation-specific initialization only after a real implementation defines it.
8. **Start / Stop Responsibility:** Use the real application's verified process-management method; none exists in this reference.
9. **Operational Verification:** Verify resulting service health, dependencies, configuration, persistence, version identity, and core flow using real evidence when a real implementation exists.
10. **Upgrade / Rollback / Removal:** Follow governed real-project procedures; this reference defines only responsibilities and criteria.

### Source-Mode Expected Topology

```text
Source runtime / application responsibility
        ↓
External PostgreSQL 16 service
        ↓
External durable database storage
```

## Docker Installation View

1. **Container Runtime Prerequisite:** Docker Engine 26+ is the fictional supported container-runtime contract.
2. **Image / Build Responsibility:** A real Project must define an image source/build process. No image or Dockerfile exists here.
3. **Configuration:** Map APP_ENV / APP_PORT and database reference semantics without changing their meaning.
4. **Secret Handling:** Use an approved external/container secret mechanism; never embed actual DB_PASSWORD in Project Source/image/config example.
5. **Persistence:** The PostgreSQL service/container requires durable volume/storage responsibility.
6. **Network / Port Model:** Application service uses conceptual port semantic `8080`; database connectivity remains internal/controlled according to real environment design.
7. **Startup Ordering:** Database availability precedes persistence-dependent application readiness.
8. **Start / Stop:** Use the real orchestration/container mechanism when one exists; no Compose/Kubernetes/Helm command is provided here.
9. **Health Verification:** Verify application and database resulting state from real runtime evidence when implemented.
10. **Logs / Diagnostics:** Define application/database log locations or collection methods in the real Project; none are fabricated here.
11. **Upgrade / Rollback:** Preserve application/data contract and manage image/database compatibility through a real governed procedure.
12. **Backup / Restore:** Protect durable PostgreSQL data through a real verified procedure.
13. **Removal / Cleanup:** Distinguish disposable application packaging from persistent data; never delete durable data implicitly.

### Docker-Mode Expected Topology

```text
Fictional application container/service
        ↓
Separate PostgreSQL 16 container/service
        ↓
Durable database-volume responsibility
```

No Docker network, volume, container, image, Dockerfile, Compose file, or command is created by this example.

## Configuration / Secret References

| Semantic | Requirement |
|---|---|
| APP_ENV | non-secret environment/profile selector |
| APP_PORT | non-secret application-port semantic; conceptual default 8080 |
| DATABASE_URL | sensitive database connection-reference semantic; actual value external |
| DB_PASSWORD | actual value must be resolved externally through SECRET-001 or equivalent real secret store |

## Verification / Health Contract

When a real implementation exists, installation is not considered operationally ready merely because an install/start operation reports success. Verify as applicable:

```text
application process/service available
PostgreSQL dependency reachable
data/storage initialized
persistent storage writable and survives expected restart behavior
required configuration loaded with correct semantics
secrets resolve without exposure
health endpoint or equivalent runtime signal succeeds
core user/system flow is usable
running version is identifiable
Source/Docker parity holds except declared variance
```

This synthetic reference supplies **no runtime evidence** for these items. EVD-001 is documentation-review evidence only.

## Logs / Diagnostics

A real Project must identify log sources, diagnostic commands/tools, retention, access authority, and evidence capture. The synthetic reference records the responsibility but no executable diagnostic path.

## Upgrade Contract

- preserve DEC-001/DEC-002/REQ-002/REQ-003 semantics unless governed change approves otherwise;
- assess database/application compatibility;
- evaluate Source/Docker parity and variance;
- update Project Source and evidence after verified real-world change.

## Rollback Contract

Rollback must define trigger, target known-good state, data compatibility, configuration rollback, persistence protection, authority, verification, and failure escalation. `ISS-001` remains OPEN because this fictional rollback documentation requires revalidation against the latest documented variance.

## Backup / Restore Contract

- protect PostgreSQL data before changes that materially risk persistence;
- identify backup scope, retention, restoration target, authority, verification criteria;
- Source mode and Docker mode may use different mechanics while preserving the same data-integrity objective.

No backup artifact or restore operation exists in this repository.

## Uninstall / Cleanup

A real procedure must distinguish removable application/runtime artifacts from retained/persistent Project data. Destructive data cleanup requires explicit authority and verification.

## Troubleshooting

A real Project should map symptoms to configuration, dependency, persistence, health, logs, and known variance. This synthetic reference intentionally contains no executable troubleshooting commands.

## Known Limitations / Deployment Mode Variance

See the **Deployment Mode Variance — Database Placement and Persistence Mechanics** sub-record in `40`. It is a local descriptive sub-record, not a new Framework Stable-ID object type. Source mode uses external PostgreSQL; Docker topology may use a separate PostgreSQL container/service. Persistence/backup/startup mechanics differ; application/data/security/configuration semantics remain shared.

## Related

`DEC-001`, `DEC-002`, `REQ-001`, `REQ-002`, `REQ-003`, `RISK-001`, `DEP-001`, `CR-001`, `GATE-001`, `ISS-001`, `EVD-001`, `SECRET-001`.
